# Dependencies
node_modules/

# Environment variables (contains Supabase credentials)
.env
.env.local
.env.production
# (.env.example IS tracked — it holds placeholders only)

# Database files
*.sqlite
*.db

# Uploaded media files
server/uploads/

# Logs
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

# Vite build cache
.vite/

# Build output (regenerated by `npm run build` + build.sh)
dist/

# Packaged extension archives
*.zip

# Snapshot of an older tree taken before the refactor. It embeds real Supabase
# service keys in several files (verify_supabase.js, src/supabaseClient.js,
# scripts/emergency_clear_v2.cjs) — never commit it.
safepost-claude-plan/backups/

# QA evidence: screenshots of real Facebook groups/posts and log trails.
# Local-only — personal data, and large binaries.
tests/artifacts/

# Scratch
.tmp/
*.repaired
